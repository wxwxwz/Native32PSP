# 更新记录

## Unreleased

- 修复 CMASTER 加载 CMSTART 后因背景音乐分配失败而退出：超过 10 MiB 的场景文件使用更小的图片缓存，为音频、画布和显示缓冲留出空间。保留原画布和解码结果；该大场景仍有明显重复解码卡顿，验证与取舍见 [CMASTER 退出修复](docs/CMASTER-EXIT.md)。

- 修复游戏中 FPS 和截图提示直接写显存、没有更新 GPU 缓存画面的问题；改为小区域 GE 上传，处理缩窄和消失后的残影。MPEG 常见系数、符号及块结束标记合并查表，脚本执行借用已缓存指令以减少复制。保持解码结果及游戏语义，测量和验证见 [性能与帧率显示](docs/DECODE-VM.md)。

- 暂停、设置、截图浏览和加载页面统一通过 GE 提交，避免 CPU 直接写显存与 GPU 缓存画面不同步；恢复游戏时同步清理 GPU 黑边。新增暂停路径日志和独立 CPU/GPU 双页回归。验证边界见 [暂停画面修复](docs/PAUSE-DISPLAY.md)。

- MPEG 改为独立的原尺寸 RGB 缓冲，小视频居中不放大，超屏视频直接由 GU 等比缩小；超过纹理上限时使用有界输出。修复 `Ord Blocks` 开始游戏后负长度字符串截取丢失候选顺序、只显示两条的问题。实现与验证见 [原尺寸视频与四条候选修复](docs/NATIVE-VIDEO.md)。

- 根据 0104 真机诊断优化 VM 常见整数解析/格式化和出栈复制；其他数值格式继续沿用原规则。MPEG 缩放时缓存列采样与共享色度，覆盖 NALOGO 的 352×288→320×240 路径，保持逐像素结果和时序。新增数值、完整解释器及 MPEG RGB 对照；实现与验证见 [脚本与 MPEG 热点优化](docs/CPU-HOTSPOTS.md)。

- 修复 YUV 相邻色度恢复后仍为零的分量被直接转换、使黑色阴影网点呈 `#009A00` 的问题；分别回退到中性色度 128，保留 `Y=0` 透明和正常绿色。修复范围及验证见 [YUV 阴影与边角绿点修复](docs/YUV-COLOR-FIX.md)，继续保留 `0104` 性能诊断与此前 MPEG 优化。

- 为剩余约 8 FPS 的反馈增加阶段耗时与工作量诊断。`20260920.0036` 设备记录显示，RCPLAY 关闭跳帧时仍有明显核心 tick 峰值，需要继续细分原因；这一版新增统计，不承诺速度改善。证据与范围见 [剩余低帧率与慢 tick 诊断](docs/FRAME-SPIKES.md)。

- 同场景滚动时先剔除屏外图像再排序；新入屏图像直接借用资源数据解码，YUV 用两条色度行替代整图插值缓冲，并融合透明边界统计，减少首次露出图块的临时分配与扫描。
- AUTO 按新发生的超载周期降档，防止单次重绘峰值被平滑耗时重复计算后长期维持低帧率；周期性和持续超载仍保留降帧保护。
- 增加重绘及图片解码耗时日志。实现与回归见 [同场景滚动与刷新峰值优化](docs/SCROLL-REFRESH.md)；后续设备记录及剩余尖峰见 [慢 tick 诊断](docs/FRAME-SPIKES.md)，仍不能宣称最低帧率问题已经解决。

- MPEG 内部预算保留最多一个 tick 的空闲余量，减少 I/P 耗时突发后连续跳过 B 帧；视频 AUTO 不再叠加游戏画面的降帧周期，保留独立视频过载保护、固定跳帧和音频时序。
- 优化 MPEG 起始码扫描、运动补偿与块输出，移除 IDCT 系数副本和重复 MP2 清零；音频重采样避免 PSP 逐采样调用 64 位整数转浮点辅助函数。
- 本轮视频帧率问题的实现、回归与主机对照见 [MPEG 低帧率优化](docs/MPEG-FPS.md)，实际 PSP 帧率及音频连续性仍待新构建真机验证。

- 游戏合成缓存透明像素包围盒，仅扫描可见范围；完整非透明首层覆盖画布时省略重复清空，保留透明洞、截断图像和绘制顺序行为。
- 精灵时间轴复用存在标记，电影帧处理复用拥有名称内容的快照，TotalFrames 直接查询缓存，减少稳定游戏循环的临时分配。
- 优化 ARGB→ABGR 转换及行遍历，删除 GU/GE 同步后重复的整画布写回，保留绘制前缓存处理和 CPU 叠加层局部写回。
- 固定跳一帧在连续两 tick 追帧时呈现最后一帧，修复奇偶相位导致持续丢画面；逻辑与音频仍逐 tick 推进。
- 本轮游戏优化的改动前真机日志、主机基准及验证状态见 [游戏循环与画面准备优化](docs/GAME-SMOOTHNESS.md)。稀疏与混合透明场景主机耗时下降，原不透明小精灵基准小幅回退；尚无本轮真机结果，不表示已发布或普遍提速。

- 常见缩放画布改用 GU 纹理呈现；原尺寸及超大画布保留对齐缓冲与 GE 复制路径。
- 显示分类增加滤波：默认清晰，可选平滑；延伸纹理边缘以避免缩放黑边。像素中心采样可能使部分清晰缩放边界较旧版相差一个源像素。
- 核心合成在解码前跳过屏外图像，并对 alpha 全部非零的图像使用可见行复制，保留原有透明规则。
- 截图按需读取当前缩放与滤波后的干净画面，排除 FPS 和截图通知；增加画面准备、传输及缓冲容量日志。
- 画面呈现优化已通过 PSPSDK 全量构建、AV/MP3/渲染回归和 AV/渲染 ASan/UBSan 检查，核心渲染摘要不变。
- MPEG DCT 系数增加约 1.5 KiB 的 8 位 VLC 前缀表，保留长码、短尾和 EOF 的逐位解码行为。
- 视频保留上一画面时不再重复准备和 GU/GE 呈现；25 FPS 合成视频的 30 个 tick 仅上传 25 次，30 Hz 音频、逻辑及播放时间保持推进。
- 流式读取按当前 PES payload 返回，避免为凑满 16 KiB 请求连续搜集多个包；截断、非法 PES 和缺失音轨按首错停止安全结束，损坏流不保证沿用旧版偶然恢复行为。
- 前一轮 MPEG 的 PSPSDK 全量构建、普通 AV、核心摘要及完整 AV ASan/UBSan 检查通过，包含损坏 PES 与缺失音轨用例；当轮完成时尚无新设备记录。后续取得的 2224 构建设备基线见 [游戏优化记录](docs/GAME-SMOOTHNESS.md)，本轮新改动的真机卡顿改善和速度仍待验证。

## 1.3.0 — PSP 整理版（2026-09-19）

- MPEG 按需读取，修复头部声明无音频但实际含音频包的静音问题。
- 优化帧调度、画面复制和资源释放；保留真机兼容性及性能待验证说明。
- 增加分类设置、深浅外观、主题颜色和外部语言文件。
- 简化暂停菜单，使用游戏原生存档；接入摇杆、截图浏览/删除及金手指。
- 完善主页计数、FPS 自适应背景、局部半透明加载提示、系统信息和关于。

这是独立 PSP 仓库的整理版，不代表所有游戏兼容性问题均已解决。
